server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jrtq2025_HIS_DB
    username: root
    password: np142060
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

ar:
  apiUrl:
    url: http://localhost:9091/SsaWebOperation/find/{ssn}

state: Pennsylvania
